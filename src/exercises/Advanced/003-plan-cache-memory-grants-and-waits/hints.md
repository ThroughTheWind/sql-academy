# Hints

1. Filter the plan-cache query by the exact comment tag from `starter.sql`.
2. Use `index_id IN (0, 1)` when you want a base table and clustered-storage view from `sys.dm_db_partition_stats`.
3. A strong hypothesis names one likely subsystem and one concrete check that could prove the first theory wrong.