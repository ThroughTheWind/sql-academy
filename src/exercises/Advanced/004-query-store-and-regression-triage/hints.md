# Hints

1. Query Store is most useful when you already know which query you care about. Filter by the tagged comment first.
2. If you want persisted history instead of in-memory state, compare this lab with the plan-cache workflow in Advanced 003.
3. Do not decide about plan forcing until you know how many plans exist for the tagged query and what evidence would make you undo the decision.