# Expected Outcomes

- the learner can point to the exact application query or loop that creates the N+1 pattern
- `rowversion` is part of the concurrency discussion, not an afterthought
- bulk ingestion is designed around staging, deduplication, and controlled publish steps