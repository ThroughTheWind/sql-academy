# Hints

1. Start by separating local-learning convenience from shared-environment defensibility.
2. A runtime that can also migrate the schema is not the same boundary as a runtime that can only serve application work.
3. Least privilege is only real when someone can prove a privileged default stopped being the easiest path.
