# Expected Outcomes

- the learner makes an explicit local-only or not-defensible decision instead of only repeating generic least-privilege guidance
- the learner identifies that `sa` runtime access and startup migration authority are separate but compounding risks
- the learner proposes one concrete separation-of-duties boundary between runtime, migration execution, and operator-admin work
- the answer includes one operational proof that a privileged default or convenience path was actually removed or gated
