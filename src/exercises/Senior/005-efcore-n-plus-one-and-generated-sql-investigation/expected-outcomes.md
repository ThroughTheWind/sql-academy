# Expected Outcomes

- the learner points to the posts read path as a projection-first, read-only EF Core query rather than an entity-materialization loop
- the learner captures or describes generated SQL as evidence instead of inferring behavior only from C# syntax
- the disposable experiment shows what repeated per-row access would look like in logs or query output
- the final write-up proposes one concrete regression guard for future posts-path refactors