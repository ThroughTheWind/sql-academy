# Hints

1. Use `ROW_NUMBER()` when you need to preserve an explicit display order through validation.
2. Filter the actionable orders before assigning the review rank.
3. Decide whether a missing required value should be rejected before you start checking uniqueness.