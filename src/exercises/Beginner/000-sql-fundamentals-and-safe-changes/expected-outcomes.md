# Expected Outcomes

- the learner uses explicit projection instead of relying on `SELECT *`
- the learner proves that `TOP` and filtering are coupled to a deterministic sort order
- the learner can explain why candidate rows would be rejected before attempting an insert