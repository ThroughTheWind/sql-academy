# Expected Outcomes

- the cohort summary returns a single January 2025 cohort with four users and three ordering users
- the first three trades are returned in deterministic descending trade order
- running trade quantities reset per user