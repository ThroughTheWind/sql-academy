# Grafana Dashboards

Drop exported dashboards into this folder as the training platform grows.

Recommended first dashboards:
- ASP.NET Core request latency, throughput, and error rate
- SQL Server query latency by endpoint and operation
- Background worker polling cadence and failure count
- Container availability and scrape health